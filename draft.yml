models:
  ExpenseCategory:
    name: string min:3 max:255
    status: nullable boolean default:true
    softDeletes: true

controllers:
  ExpenseCategory:
    store:
      validate: code status
    update:
      validate: code status

inertia_vue:
